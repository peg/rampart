# Rampart Review Findings

_Generated by GPT-5.4 via Codex CLI — 2026-03-13_

## 1. Docs Website Review

### Broken Links
- Homepage links to `reference/owasp-mapping.md#response-scanning-owasp-risk-3` — anchor doesn't exist
- CTA link to `reference/cli-commands.md#rampart-init---from-audit` — heading slug doesn't match

### Contradictions
- **Homepage vs OWASP page**: `index.md` says "2 fully covered, 6 partially mitigated" but `owasp-mapping.md` says "2 fully covered, 7 partially mitigated, 1 not addressed" — homepage is wrong
- **Claude Code guide**: says "The default action is `watch`" but `standard.yaml` uses `default_action: deny`
- **Webhooks page**: says `action: webhook` is fail-open by default — code says the opposite (`fail_open` defaults to `false`)
- **Policy engine page**: says deny in `custom.yaml` overrides allow in `standard.yaml` "and vice versa" — the "vice versa" is false, engine is deny-wins

### Version Drift
- `docs-site/index.md` says v0.8.1
- `docs-site/reference/threat-model.md` says "Applies to: v0.8.x"
- `README.md` says v0.9.0
- `CHANGELOG.md` tops out at 0.7.1

### Missing Pages
Guides referenced from README but missing from docs site: `native-ask`, `ci-headless`, `project-policies`, `benchmarking`, `windows`, `wazuh-integration`

---

## 2. OWASP Mapping Audit

### Correct
- All 10 risk IDs (ASI01–ASI10) are present and match the official OWASP 2026 list
- Names and descriptions align with the official framework

### Overstated
- **ASI05 (RCE) marked as ✅ Covered** — should be ⚠️ Partial. Rampart's own threat model admits it cannot inspect what `python3 script.py` does internally, and LD_PRELOAD doesn't apply in native-hook mode
- **ASI08 (Cascading Failures)** — claims fail-open design, but important paths are actually fail-closed (parse failures in enforce mode deny, webhook actions default fail-closed)
- **ASI03 (Identity & Privilege)** — overstates by treating user separation as a Rampart control; it's deployment guidance, not enforced
- **ASI04 (Supply Chain)** — weakly argued. Project-local policies can widen permissions where base policy is silent

### Understated
- The mapping underplays protections already in `standard.yaml`: sensitive file blocking, `sudo` approval, `dscl` user-management blocking, response-side secret detection

---

## 3. Repo vs Docs

### Stale Numbers
- `standard.yaml` has **45 policy entries and 79 rule entries** — docs/benchmarking guide says "47 rules"

### Semantic Drift
- `watch-prompt-injection` now uses `action: ask` in code, but docs describe it as watch/monitor-only
- `require_approval` was migrated to `action: ask` per CHANGELOG, but docs still present `require_approval` as primary
- `command_contains` — code comments say "ALL specified substrings" but `matcher.go` uses OR semantics

### Verified Correct
- `rampart mcp scan` exists in code ✅
- HMAC-signed approval URLs exist ✅
- Response scanning is real and implemented ✅

---

## Summary

The biggest problems are **security-relevant inaccuracies**, not typos:
1. Wrong webhook fail-open/fail-closed default
2. Wrong OWASP summary count on homepage
3. Wrong/muddled `default_action` language
4. Overstated ASI05 coverage (should be Partial)
5. Version numbers are incoherent across 4 locations
